console.info("page loaded");
console.debug(2 + 3);
console.warn(2 * (3 + 1));

var employed = true;
console.error(employed);

var age = 24; //number

var user = "Andru";
user = user + " Pirvulescu ";

var Skills = ["html", "css", "js"];

var dan = {
  age: 26,
  employed: true,
};

var jobtitle = document.getElementById("job-title");
